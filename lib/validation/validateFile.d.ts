import NormalizedPath from '../types/NormalizedPath';
import { SourceFileProvider } from '../core/SourceFileProvider';
export default function validateFile(filePath: NormalizedPath, fileProvider: SourceFileProvider): Promise<void>;
